function toggledivA(athleticButton) {
    var div = document.getElementById(athleticButton);
    div.style.display = div.style.display == "block" ? "none" : "block";
}
function toggledivB(greekButton) {
    var div = document.getElementById(greekButton);
    div.style.display = div.style.display == "block" ? "none" : "block";
}
function toggledivC(studentgovButton) {
    var div = document.getElementById(studentgovButton);
    div.style.display = div.style.display == "block" ? "none" : "block";
}
function toggledivD(mediapoliticsButton) {
    var div = document.getElementById(mediapoliticsButton);
    div.style.display = div.style.display == "block" ? "none" : "block";
}
function toggledivE(serviceButton) {
    var div = document.getElementById(serviceButton);
    div.style.display = div.style.display == "block" ? "none" : "block";
}