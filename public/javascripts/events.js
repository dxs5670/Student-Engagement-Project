function toggledivA(eventsButton) {
    var div = document.getElementById(eventsButton);
    div.style.display = div.style.display == "block" ? "none" : "block";

function toggledivA(stateButton) {
      var div = document.getElementById(stateButton);
      div.style.display = div.style.display == "block" ? "none" : "block";
